<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/recycler_category_item_container"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/default_app_margin"
    android:layout_marginTop="@dimen/default_app_margin"
    android:layout_marginBottom="@dimen/default_app_margin">

    <androidx.cardview.widget.CardView
        android:id="@+id/recycler_category_item_card_view"
        android:layout_width="140dp"
        android:layout_height="90dp"
        app:cardCornerRadius="@dimen/default_app_radius">

        <ImageView
            android:id="@+id/recycler_category_item_image_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/colorSecondary"/>

        <TextView
            android:id="@+id/recycler_category_item_text_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textAppearance="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:textSize="@dimen/medium_text_size"
            tools:text="Apartment" />

    </androidx.cardview.widget.CardView>

</LinearLayout>